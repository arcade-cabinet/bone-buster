# Continuous Work Directive — objexoom

**Status:** ACTIVE

## Phase 9 — wire the remaining staged COV pools

Step-1 of COV8 / COV11 / COV12 / COV13 / COV14 each staged an asset
pool + a `pickXxx(hash)` deterministic picker. None are mounted in
the renderer or consumed by gameplay yet — all 5 are dormant data.
Phase 9 wires each to a real game-loop use.

- [x] **COV8 step-2 — trap tick-damage + lever disarm.** Shipped. New `src/scatter/trapScatter.ts` exports `TrapInstance` + `spawnTraps(map)` + `trapAt` + `triggerAt` + `disarmSector` + damage/cooldown/radius constants. Scatter is archetype-biased: arena & sewer lean heavy (1-2 hazards/sector), corridor/courtyard mid (0-1), library light. Each sector that gets ≥1 hazard ALSO gets 1 lever (trigger-kind) so the player has a way to disarm it. PRNG seed `map.seed XOR 0x54524150` ("TRAP" tag — diverges from every other scatter). 3-tile skip-radius from spawn/exit/key, 2.0-tile inter-trap spacing. New `src/scene/entities/TrapField.tsx` renders cloned GLBs; disarmed hazards drop their group `visible` (triggers stay visible after use as a visual breadcrumb). ObjexoomScene's per-frame loop now: (1) checks triggerAt → walks → flips disarmed on every trap in the same sectorId via disarmSector; (2) checks trapAt → applies damage via gameRef.onHit using per-kind damage table (spike=5, blade=7, rolling=10) gated by a per-trap `lastTrapTickAt` Map and 500ms cooldown; playHurt() fires on tick. 13 new unit tests pin: scatter determinism, grid-maps-return-empty, arena-density-non-zero, skip-radius-respected, yaw-in-range, id-uniqueness, disarmed-starts-false, disarmSector-flips-only-matching-sector, disarmSector-idempotent, trapAt-finds-hazards-ignores-disarmed-and-triggers, triggerAt-only-matches-triggers, trapAt-returns-null-when-no-overlap. 439 unit + 5 browser + 5 e2e canonical screenshots green. Canonicals byte-stable (refLevel 0 uses loadRefLevel which has no sectors that trip the 3-tile skip-radius and sample inside; the canonical poses don't overlap any trap centroid).

- [x] **COV12 step-2 — rare loot pickup spawns.** Shipped. Extended `PickupKind` with `"loot"` — exactly one loot pickup per sector map at the farthest-sector centroid (mirrors COV10 wreck placement). New `src/scatter/lootScatter.ts` exports `pickLootSpawn(map)` + `lootPickupSpawn(map)`. The lootKind (bottles/books/treasure) is resolved via `pickLootKind(map.seed)`. ObjexoomScene appends the loot to the base `spawnPickups(map)` output with a stable id (= base.length) so collected/uncollected tracking is consistent across reloads. `PickupMesh` gained a new `LootPickupMesh` sub-component that mounts the COV12 GLB (Bottles/Books/Treasure) at 0.6 scale via SkeletonUtils.clone; all 3 GLBs preloaded on module evaluation. `ObjexoomShell.onCollectPickup` extended with a `loot` branch that resolves `pickLootKind(seed)` and applies the bonus: bottles → +5 HP (clamped), books → +chaingun + +shotgun pickup-amount ammo, treasure → +5 kills (score proxy). Updated `ammoIncrement` Record (exhaustiveness check) with a `loot` sentinel + handler. 7 new unit tests pin: returns-null-for-grid-maps, farthest-sector-centroid-placement, kind-cycles-deterministically (bottles/books/treasure across seeds 0/1/2), same-seed-same-spawn, empty-sectors-null, lootPickupSpawn-wraps-as-PickupSpawn-loot. 446 unit + 5 browser + 5 e2e canonical screenshots green. Canonicals byte-stable (refLevel 0 uses loadRefLevel and its 3 sectors place the farthest-centroid loot far from the spawn-anchored canonical poses).

- [ ] **COV13 step-2 — kitchen archetype scatter.** The kitchen prop pool was sized for set-dressing — wire it as a 6th archetype OR as a sector-level dressing layer for library/courtyard. Pragmatic call: add it as a new sub-scatter that fires on ~20% of library-archetype sectors (food/dining areas in a library make sense — break room, study hall). New `spawnKitchen(map)` deterministic scatter (1-3 props per opted-in sector); `<KitchenField>` renderer; sits alongside PropField. 8+ unit tests.

- [ ] **COV11 step-2 — courtyard nature scatter.** `Mega_Nature.glb` is a single aggregate scene. Use `SkeletonUtils.clone` to extract 3-4 named sub-meshes (a tree, a shrub, a rock, etc — identify via Object3D.name traversal). New `spawnNature(map)` deterministic scatter: only fires on courtyard-archetype maps; places 4-8 nature instances per sector (denser than other scatters — courtyard reads as "outdoors"). `<NatureField>` renderer mounts the cloned sub-meshes. 8+ unit tests pin: archetype gating, named-mesh resolution, determinism, sector-containment.

- [ ] **COV14 step-2 — ambient NPC spawns.** Extend `EnemyKind` with `"npc"` (no aggro, no LOS, no attack; FSM short-circuits in state 0 patrol forever). `spawnEnemies` opt-in: ~15% of enemy slots in library archetype become NPCs instead of skeletons/wraiths/imps (the "library is inhabited by quiet researchers" read). NPC variants picked via `pickNpcKind(seed XOR slotIndex)`. 10+ unit tests pin: spawn ratio, kind distribution, FSM-stays-patrol invariant, no-damage invariant.

## Phase 8 — closing PRD §E13 visual-distinctness gap

Forward-sweep from Phase 7: I screenshotted all 5 archetypes (INF4)
and judged them "tinted variants of the same place," not "structurally
different places" as PRD §E13 demands ("Visual identity test: a
screenshot of each archetype is distinct to the eye"). E13 step-2
only changed `<ambientLight>` + `<directionalLight>` colors — the
fog still pulls from a single literal `OBJEXOOM_PALETTE.ink` across
every archetype. Fog is the dominant depth-fade signal in low-lit
play; tinting it per-archetype gives a much larger visual delta than
the ambient/directional tweaks did.

PRD §E13 lists 5 axes per archetype. Phase 8 closes the remaining
visual gaps (fog tint + sector density). The other axes (props,
enemies, lighting palette, SFX) are already shipped — these two
are the diff between "distinct to the eye" and what I shipped.

- [x] **E13 step-4 — per-archetype fog tint.** Shipped. Extended `ArchetypeLightPalette` with a third color: `fogColor`. The corridor entry preserves `OBJEXOOM_PALETTE.ink` for canonical byte-stability. The 4 other archetypes pick fog colors that match identity: arena `SCALE.ember[900]` (smoke/heat haze on depth fade), courtyard `SCALE.indigo[900]` (dusk-cool, separates hardest from corridor), sewer `SCALE.parchment[900]` (damp underground, parchment-deep), library `SCALE.amber[900]` (paper/dust mote sepia depth fade). ObjexoomScene's `<fog>` element now reads `lightPalette.fogColor`. Self-judgement after re-capture: arena reads ember-rim with smoke fade, library reads cool-warm sepia split, sewer reads as a clear wider corridor with warm-haze vanishing point, courtyard reads dusk-blue, corridor stays the canonical violet+parchment+ink. Visually distinct to the eye — closes the PRD §E13 "Visual identity test" acceptance criterion. Two new test assertions: corridor's fogColor preserves `OBJEXOOM_PALETTE.ink`, and ≥3 archetypes have unique fog colors (depth-fade separation). Also extended the existing "every entry has valid hex" test to cover fog. 418 unit + 5 browser + 5 e2e canonical + 5 e2e archetype screenshots green. Canonicals byte-stable (corridor's fogColor unchanged).
- [x] **E13 step-5 — per-archetype sector density / size.** Shipped. New `src/archetypeMapShape.ts` exports `ARCHETYPE_MAP_SHAPES` (5-entry Record keyed on `PropArchetype`) + `getArchetypeMapShape(archetype)`. Each entry carries `{minRoom, maxRoom, roomTries}`. corridor: `{3, 6, 90}` (pre-step-5 defaults — canonical byte-stability), arena: `{5, 8, 50}` (sparse big rooms), courtyard: `{3, 7, 75}` (mixed), sewer: `{2, 5, 120}` (winding narrow), library: `{3, 5, 130}` (grid-like stacked smaller). `generateMap(seed, shape?)` extended with an optional `shape` arg; falls back to MIN_ROOM/MAX_ROOM/ROOM_TRIES constants when absent (back-compat). `buildMap` now looks up the archetype from the seed and passes the matching shape into `generateMap` for procedural builds — refLevels go through loadRefLevel unchanged. Visual self-judgement on re-captured archetype screenshots: arena reads as "wide open combat space with sparse rooms" (HUD: 0/6 enemies), library reads as "dense cluster of tightly-packed small rooms" (HUD: 0/12 enemies), corridor stays canonical (0/9). The HUD enemy count alone shows the structural difference. 8 new unit tests pin: every archetype has a shape, corridor preserves defaults, shape invariants (minRoom ≤ maxRoom, positive roomTries), ≥3 distinct shape signatures, `generateMap(seed)` byte-identical to `generateMap(seed, corridorShape)`, arena rooms respect minRoom=5, library rooms respect maxRoom=5, sewer minRoom=2, and determinism. 427 unit + 5 browser + 5 e2e canonical + 5 e2e archetype screenshots green. Canonicals byte-stable (refLevel 0 uses loadRefLevel, not generateMap, so this change has zero effect on canonical poses).

## Phase 7 — visual self-judgement infra + archetype coverage

Forward-sweep from Phase 6: E13 step-2 tinted lights per archetype,
COV10 step-2 placed a wreck in courtyard archetype, but neither
appears on the refLevel-0 canonical screenshots (which use corridor
archetype). Right now the agent has no way to visually self-judge
the 4 non-corridor archetypes — CLAUDE.md's "screenshot, read your
own screenshot, compare to spec" loop can't run for them. That
is itself a process bug. Phase 7 fixes the infra first, then sweeps
the resulting visual evidence for regressions.

- [x] **INF3 — `?objexoomArchetype=<name>` URL override.** Shipped. ObjexoomShell now reads `?objexoomArchetype=<name>` and applies `applyArchetypeOverride(baseSeed, archetype)` on top of the base seed (which may itself come from `?objexoomSeed`). The rewrite uses `seed - (seed % 5) + ARCHETYPE_NAMES.indexOf(name)` so `pickArchetype(map) === name` for every input seed. Unknown / absent archetype names fall through unchanged. The function is exported from ObjexoomShell so the unit test can pin the invariant. 4 new unit tests: seed-unchanged-when-null, seed-unchanged-when-unknown, `pickArchetype === name` for every (5 archetypes × 9 seeds) combination, and idempotency (applying twice is a no-op). CLAUDE.md "Notes" gained a one-paragraph entry describing the flag, the stacking-with-objexoomSeed semantics, and when to use it (visual self-judgement of the 4 non-canonical archetypes). 407 unit + 5 browser + 5 e2e screenshots green. Canonicals byte-stable (the override only fires when the URL param is present; the default seed path is unchanged).
- [x] **INF4 — per-archetype canonical screenshots.** Shipped. New Playwright spec `tests/e2e/archetypeScreenshots.spec.ts` captures one ingame screenshot per archetype (5 total: corridor, arena, courtyard, sewer, library) using `?objexoomDebug&objexoomSeed=12345&objexoomArchetype=<name>`. Written to `test-results/objexoom-archetype-screenshots/<archetype>.png` (gitignored — matches the existing screenshots.spec.ts pattern). New `pnpm test:e2e:archetype-screenshots` script. **MID-DRAFT FIX:** the first run produced 5 visually-identical shots — investigation found the default `level: 1` setting loads the baked refLevel 0 (corridor) regardless of the seed, so the archetype override was silently no-oping. Fixed by switching the initial `settings.level` to `"procedural"` when `?objexoomArchetype` is present in the URL. After fix, all 5 archetypes render visually distinct: corridor reads violet+parchment, arena reads blood-tinted, courtyard cool+violet, sewer purple+indigo, library blue+ember-rim. Self-judgement complete; INF4 baselines are now the visual gate for future E13 / COV10 / palette work. 407 unit + 5 browser + 5 e2e canonical screenshots green + 5 e2e archetype screenshots captured. Canonicals byte-stable (the procedural-when-archetype-present override only fires on the new URL param; default boot path unchanged).
- [x] **PR #16 reviewer fold-forward.** Shipped. Folded Gemini + CodeRabbit + github-code-quality feedback from PR #16 review into a single commit. Substantive bug fixes: (a) **dead-enemy yuka cleanup was unreachable** in enemyTickLoop.ts:103 because the `if (enemy.dead) continue;` early-return sat ABOVE the yuka cleanup at line 193 — fixed by moving the cleanup to the top of the dead branch; (b) **O(N²) get-help fanout** in the enemy loop — fixed by precomputing `enemyById = Map<number, Enemy>` once per tick and replacing `allEnemies.find((e) => e.id === helpId)` with `enemyById.get(helpId)`; (c) **chaingun rotation comment was incorrect** — Three.js camera-forward is -Z not -X, so the comment claiming "rotation_Y=π flips +X to -X = camera-forward" was wrong. Comment rewritten to reflect that muzzleBboxFrac picks in the BBOX frame (pre-rotation) and WeaponViewmodel handles the world projection at runtime; (d) **DECISIONS.md field name mismatch** — `muzzleOffset: [x, y, z]` (absolute units) was the original draft; actual implementation uses `muzzleBboxFrac: [x, y, z]` (bbox fractions). Updated the call section to match reality + clarified why bbox-frac was chosen over absolute units; (e) **decisions.ndjson reasons truncated** — 4 rows had `no-visual-impact` reasons cut mid-sentence because the global update-cursor.mjs hook's regex captures only one line of `//` continuations. Backfilled all 4 rows with full multi-line reasons pulled from git history; (f) **directive lane-priority stale pointer** — line 75 said "Next executable: PA-MOD7" but PA-MOD7 is `[x]`. Updated. **Acknowledged-not-fixed**: the muzzle-light 1-frame lag (Gemini medium-priority) is functionally invisible against the 250ms decay window; added an explanatory comment instead of restructuring to positive-priority useFrame. **Stale comment dropped**: github-code-quality's "decalsRef wasted initializer" applied to a pre-COV6-step-2.1 snapshot — the current code reads the initializer at render-time. **Markdown lint**: added `text` language tags to two fenced code blocks (ASSET_PROVENANCE.md:19, directive.md:88). **ASSET_PROVENANCE wording**: line 54 overstated "every output is referenced" — corrected to acknowledge that loose enemy GLBs from references/ are staged but not all wired yet. 417 unit + 5 browser + 5 e2e canonical green; canonicals byte-stable. (Skipped: Android scaffolding noise — hardcoded package names, missing WebView ids, etc. — that's stock Capacitor leftover and lives in its own future hardening cycle.)

- [x] **E13 step-3 — per-archetype enemy mix.** Shipped. New module `src/enemyMix.ts` exports `ENEMY_MIX_WEIGHTS` (Record<PropArchetype, [skeleton, wraith, imp]>) + `remapEnemyMix(spawns, archetype, seed)`. Weight tuples: corridor pass-through [0,0,0] (preserves pre-step-3 byte-stable distribution), arena [3,1,5] (imp-heavy), courtyard [4,3,2] (balanced), sewer [2,5,2] (wraith-heavy), library [4,4,1] (skirmish). Deterministic seeded picker (PRNG seed `map.seed XOR 0x454E4D58` "ENMX" tag → diverges from every other scatter sequence). `spawnEnemies` extended with an optional `spawnsOverride` param that ObjexoomScene now uses. The remap preserves position, order, and total count — only the `kind` field changes. Boss-pick logic is unaffected because `pickBossSpawnIndex` reads position only. 10 new unit tests pin: count-preserved, position-preserved, corridor-is-pass-through (same reference), determinism, seed-divergence, kind-validity, sewer-leans-wraith (sample 500), arena-leans-imp (sample 500), weight-tuples-are-length-3, empty-input. 417 unit + 5 browser + 5 e2e canonical + 5 e2e archetype screenshots green. Canonicals byte-stable (corridor archetype → pass-through, so refLevel 0 enemy distribution is unchanged).

## Phase 6 — gameplay extensions for staged pools

The asset-pool step-1 commits shipped data but no gameplay hooks. The
remaining step-2/3 work turns passive variety into actual game-loop
behavior.

- [x] **COV2 step-2** Shipped anchor-piece large-prop scatter. New `src/scatter/largePropScatter.ts` exports `spawnLargeProps(map)` + `LargePropInstance` type. Places 1-2 anchors per sector (sparser than E3's 2-5 and COV5's 3-5) via deterministic seeded rejection sampling (PRNG seed `map.seed XOR 0x4C415250` "LARP" tag → diverges from LMP/PROP/FLRT/DEBR/decal-FNV sequences). 5-tile skip-radius from spawn/exit/key (vs 4 elsewhere — anchor pieces are too physically large to risk blocking critical-navigation corridors); 2.5-tile inter-prop spacing so two anchors don't read as one mass. Extended `LargePropDef` with `blockingRadius` (0.8 for machinery, 0.9 for shipping container; 0 for the 8 non-blocking entries). New `blockerCirclesOf()` filter exposes the blocker circles to collision. New `src/scene/entities/LargePropField.tsx` renders one SkeletonUtils.clone per instance under a yawed group. `CollisionContext` gained an optional `blockers` field; `resolveCollisionAny` now runs a 3-iteration circle pushout (`pushOutBlockers`) after the wall pushout so the player can't end up inside a blocking anchor even on a corner-into-blocker desired move. ObjexoomScene wires `largePropsRef` + `largePropBlockers` (memo'd at mount) into `collisionCtxRef`. 12 new unit tests across two suites pin: scatter determinism, seed-divergence, ≤2-per-sector cap, id uniqueness, def membership, yaw bounds, 5-tile skip-radius, real-ref-level placement, blockerCirclesOf filtering, and 3 collision-pushout cases (push-out, leave-alone, back-compat-when-absent). 397 unit + 5 browser + 5 e2e screenshots green. Canonicals byte-stable (5-tile skip-radius keeps anchors out of refLevel-0 spawn FOV).
- [x] **COV10 step-2** Shipped courtyard-archetype RV wreck placement. New `src/scene/entities/VehicleWreck.tsx` renders one wrecked-vehicle GLB picked via `pickVehicleUrl(map.seed)` with a seeded yaw (LCG → [0, 2π)) so the wreck rests at a different angle per map. ObjexoomScene computes a `wreckPosition` useMemo that returns null when `archetype !== "courtyard"` else the centroid of the sector farthest from `playerSpawn` (one wreck per map, naturally tucked away from spawn so it reads as discovery). Conditional JSX mount: `{wreckPosition && <VehicleWreck ... />}`. All 5 VEHICLE_VARIANTS preloaded on module evaluation; SkeletonUtils.clone per mount. The wreck is collision-flat — visual weight is the point, not the obstacle. 383 unit + 5 browser + 5 e2e screenshots green. Canonicals byte-stable (refLevel 0 → seed 0 → corridor archetype, so the wreck doesn't appear in canonical poses).
- [x] **E13 step-2** Shipped per-archetype lighting palette tint. New `src/lighting/archetypePalette.ts` exports `ARCHETYPE_LIGHT_PALETTES` (5-entry Record keyed on `PropArchetype`) + `getArchetypeLightPalette(archetype)`. Each entry carries `ambientColor` + `directionalColor`. The corridor entry preserves the pre-step-2 literal values (`OBJEXOOM_PALETTE.violet` ambient, `OBJEXOOM_PALETTE.parchment` directional) so refLevel 0 → seed 0 → corridor archetype keeps refLevel 0's canonical screenshots byte-stable. The 4 other archetypes get distinct hue pairs: arena gets blood/ember (combat pit read), courtyard gets indigo/amber (outdoor cool ambient + warm sun), sewer gets parchment-mid/indigo-light (damp underground), library gets parchment-light/amber-soft (paper + dust mote sunshafts). ObjexoomScene memos a `lightPalette` from the archetype and passes the colors to `<ambientLight>` + `<directionalLight>`. Intensity multipliers are untouched — only the tint changes per archetype this step. 6 new unit tests pin: every archetype has an entry, corridor preserves pre-step-2 literals, all entries have valid hex colors, ambient ≠ directional within each archetype, ≥3 unique ambient colors across the palette, and `getArchetypeLightPalette` returns referentially-equal objects per call. 403 unit + 5 browser + 5 e2e screenshots green.

## Phase 5 — step-2 elevations (use the staged pools)

ELEVATION-P5 wires the step-1 asset pools into actual gameplay /
renderer paths so the in-game experience reflects the variety the
asset pools enable.

- [x] **COV7 step-2** Swapped RealDoor's procedural boxGeometry for the GLB picked by `pickDoorUrl(mapSeed)`. RealDoor takes a `mapSeed: number` prop; the H7 exit door uses `map.seed` and the H8 spawn door uses `map.seed XOR 0x676f6e67` ("gong" tag) so the two doors on the same map render as different variants of the 6-variant pack. The slide-upward animation now drives the group's Y position with the cloned GLB scene riding along. SkeletonUtils.clone per mount; all 6 door variants preloaded on module evaluation. 369 unit + 5 browser + 5 e2e screenshots green. Canonical refs refreshed (the door GLB sits at the exit position, offscreen from spawn-anchored poses; the variant variety surfaces at runtime once the player walks toward the exit).
- [x] **COV9 step-2** Wired `pickMeleeSkin(map.seed)` into WeaponViewmodel. When `weapon === "melee"`, the GLB URL is resolved via the picker; other weapons resolve to their single canonical URL. ObjexoomScene now passes `mapSeed={map.seed}` into WeaponViewmodel. All 7 melee skins preloaded on module evaluation so per-seed swaps don't stall the BLADE viewmodel. Back-compat: seed 0 → machete (index 0), so refLevel 0's canonical screenshots stay byte-stable. 369 unit + 5 browser + 5 e2e screenshots green.
- [x] **COV5 step-2** Shipped sector-body debris scatter. New `src/scatter/debrisScatter.ts` exports `spawnDebris(map)` + `DebrisInstance` type. Places 3-5 debris piles per non-spawn sector via deterministic seeded sampling (PRNG seed `map.seed XOR 0x44455242` "DEBR" tag — diverges from lampScatter/propScatter/floorTiles sequences). 4-tile skip-radius from spawn/exit/key. No inter-debris min-spacing (clusters look natural). New `src/scene/entities/DebrisField.tsx` renders one cloned mesh per instance with yaw. ObjexoomScene mounts `<DebrisField>` alongside `<PropField>`. 7 new unit tests pin determinism, polygon-containment, skip-radius, id uniqueness, URL membership in DEBRIS_VARIANTS, grid-maps-empty. 376 unit + 5 browser + 5 e2e screenshots green. Canonicals byte-stable (debris is in non-spawn sectors per the 4-tile skip-radius).
- [x] **COV6 step-2** Shipped wall-face decal scatter (data layer). New `src/scatter/decalScatter.ts` exports `spawnDecals(map)` + `DecalInstance` with FNV-1a tile-hash per `(sectorId, edgeIdx, slot)` so each face has a stable independent decal choice. 0-2 decals per edge based on hash + edge length; ≥3 per sector across edges in practice. Each instance carries position (with 0.05 inward-normal offset), Y (0.4-0.7 of wall height), yaw (aligned to wall face), and a pickDecalUrl-resolved GLB URL. New `src/scene/entities/DecalField.tsx` renders one cloned mesh per instance. ObjexoomScene computes the scatter and mounts <DecalField>, but the JSX is gated `{false && ...}` for this commit — empirical visual check showed the decal GLBs are full mesh exports (posters/graffiti with their own scale + origin from Blender), not flat decal quads, and rendering them raw at sector edges produced view-blocking artifacts. Step-2.1 will bound each decal to a wall-quad billboard using the GLB's texture rather than the raw mesh. The scatter data + tests + renderer plumbing stay live as the asset-enabler so step-2.1 is a clean material/geometry swap. 7 new unit tests pin determinism, URL membership, id uniqueness, Y bounds, grid-maps-empty, yaw finiteness. 383 unit + 5 browser + 5 e2e screenshots green. Canonicals byte-stable.

- [x] **COV6 step-2.1** Replaced raw-GLB DecalField with a wall-quad billboard. Each decal renders as a 1.2×0.8 `<planeGeometry>` with `meshStandardMaterial` whose `map` is the GLB's primary diffuse texture extracted via a one-shot `traverse()` walk. `transparent + alphaTest=0.5 + side=DoubleSide` so the alpha cutout reads clean. The plane is positioned + yawed via the scatter's per-instance data, aligned to the sector edge normal. Re-enabled `<DecalField>` in ObjexoomScene. 383 unit + 5 browser + 5 e2e screenshots green. Canonicals stay byte-stable (decals live on non-spawn-anchor wall faces).
**Owner:** Claude
**Mandate:** "you are to treat NOTHING as pre-existing I WANT A FULLY POLISHED PLAYABLE GAME PORTED FROM THE REFERENCE DOOM CLONE" — carried over from the original `objexiv/objexiv@feat/objexoom-easter-egg` directive.

**Branch strategy (2026-05-14, user-directed, supersedes the per-item-branch experiment):** all work stays on the current long-running branch. Commit freely; never open a new branch to "isolate" a directive item. Never open a new PR while one is already in flight for this work — fold reviewer feedback into the next forward commit on the same branch. The user will squash-merge the whole branch when ready. **Do NOT:** create new branches, open new PRs, suggest splitting work across branches, or stop work to "wait for review." Reviewer feedback is folded forward on the same branch and the loop continues.

**Spec authority:** [`docs/PRD.md`](../docs/PRD.md) is the comprehensive remaining-work spec — user stories, acceptance criteria, asset paths, dependency DAG. This directive is the executable checklist mirror of the PRD; when they disagree, PRD wins and the directive gets updated.

**Reference codebases:** `~/src/reference-codebases/` holds local clones of upstream libraries and game references used for code/doc/example consultation (NOT modified, NOT linked from this repo). The two that matter here:

- `~/src/reference-codebases/yuka/` — Yuka steering/AI library source. Consult when wiring enemy behaviors (seek/flee/path-follow), debugging `yukaIntegration.ts`, or pulling example patterns from the upstream `examples/` dir.
- `~/src/reference-codebases/js13k2019-yet-another-doom-clone/` — the reference DOOM clone that PARITY items mirror. Consult for: sprite-billboard math, weapon timings, hit-feedback patterns, level-gen seed conventions, the exact mechanics PARITY items are restoring.

Prefer reading these LOCAL clones over web search or training recall. They're the source of truth when "what does Yuka actually do?" or "how does the reference clone do X?" comes up.

## What CONTINUOUS means

1. Never stop for status reports the user didn't ask for.
2. Never stop for scope caution.
3. Never stop to summarize — git log is the summary.
4. Never stop for context pressure — task-batch + PreCompact handle it.
5. Never stop because a task feels big — pick the next atomic commit.
6. Only stop on: explicit user halt, red CI blocking, or genuine STOP_FAIL.

## Operating loop

while queue has [ ] items: implement → verify → commit → dispatch reviewers → mark [x] → next.

## Lane priority (read this before picking any item)

**Hard rule: NO item in this directive may block on external state.** Every acceptance criterion is fully autonomously verifiable from this machine. Items that previously required device/CI/human verification have been rewritten — the "device launch / live URL / human eyeball" portion is OUT of the acceptance for ship-readiness; if it matters later it ships as a manual smoke ticket, never as a directive blocker.

Pick the topmost `[ ]` item from the highest-priority lane that has remaining work:

1. **PARITY** — reference-clone gaps (all currently `[x]`; if any regress, fix first). PA-MOD7 + the rest of the PARITY items are done; this lane is currently empty.
2. **ARCH** — architectural maintenance raised by reviews. **ARCH2 (re-decomp) blocks E3.** Pick first if it's the gate.
3. **ELEVATION Phase 2** — mechanical (E5 ✅ shipped; E6 next).
4. **ELEVATION Phase 3** — visual. Order: PA-MOD7 → COV1 → E4 → COV4 → E3 → COV3 → E2. (Lane-interplay table below explains why.)
5. **ELEVATION Phase 4** — polish (E13, E7, E8, E11, E10). E13 depends on COV3.
6. **COV*** — 3DPSX coverage NOT paired with an E. Requires `/Volumes/home/assets/3DPSX/` mounted. NAS mount is verified at session start; if it goes away mid-run, remount or skip the COV lane and pick the next executable item from a different lane.
7. **INFRA** — INF2 (build-time asset copy).
8. **Standalone hardening** — B1.7, B2.1, B2.4, AO.4, AO.5, AO.6, DS.* (all parallel; pick any).

Within a lane, pick the topmost unchecked. Inside each item's checkbox the acceptance criterion is a one-liner; long-form spec is [`docs/PRD.md`](../docs/PRD.md).

## Executable next-pick algorithm (zero ambiguity)

```text
for lane in [PARITY, ARCH, ELEVATION-P2, ELEVATION-P3, ELEVATION-P4, COV, INFRA, HARDENING]:
    for item in lane.unchecked_in_order:
        if item.requires_nas and not nas_mounted():
            attempt_remount()         # one shot
            if still not mounted: continue
        return item                   # this is the next pick
return "queue drained — flip Status to RELEASED"
```

The anti-stop hook's `[WAIT-*]` escape hatch exists for emergencies (a remote dep genuinely cannot be made local). The standing rule is: rewrite the acceptance criterion until it IS autonomously checkable. `[WAIT-*]` should be empty in steady-state — and currently is.

## Lane interplay (deduplication of overlapping items)

Some COV* items overlap E* items. The COV* row is the **asset-prereq enabler**; the E* row is the **gameplay feature consuming the assets**. Always land COV before its paired E in that pair.

| COV (assets) | E (feature) | Relationship |
|---|---|---|
| **COV1** Light Sources (10 GLBs) | **E4** Lit lamps + shadow projection | COV1 stages the variants; E4 wires the pointLights + shadow maps. **COV1 first.** |
| **COV3** Modular Structures (210 GLBs) | **E13** Procedural archetype deepening | COV3 stages the tile palette; E13 reads them through archetype configs. **COV3 first.** |
| **COV4** Props (137 GLBs) | **E3** Decorative sector scatter | COV4 stages the scatter pool; E3 wires per-archetype curation. **COV4 first.** |
| **COV9** Melee variants (axe/knife/etc) | **E1** Melee slot (BLADE shipped) | E1 shipped with machete; COV9 adds seeded variant cycling. **E1 done; COV9 is the extension.** |
| **COV8** Traps (20 GLBs) | **E6** Switches + secrets | Independent — traps are hazards, switches disable them. Either order works; pick the topmost. |

All other COV* items have no E* pair and run standalone.

## Forbidden phrases

"deferred" | "v2+" | "out of scope" | "future work" | "tracked separately" | "follow-up"
"TODO" | "FIXME" | "stub" | "placeholder" | "mock for now"
"pause point" | "natural pause" | "fresh session" | "next session" | "stopping point" | "clean handoff" | "ready to hand off"

## Queue — Standalone repo bring-up

### B0 — Initial commit + remote

- [x] **B0.1** First commit on `main` capturing the extracted source + scaffolding.
- [x] **B0.2** Create `objexiv/objexoom` GitHub repo (internal visibility).
- [x] **B0.3** Push `main` to origin.

### B1 — Verify it actually runs

- [x] **B1.1** `pnpm install` succeeds.
- [x] **B1.2** `pnpm check` (tsc no-emit) green.
- [x] **B1.3** `pnpm lint` (biome) green.
- [x] **B1.4** `pnpm dev` boots Vite, root page mounts `<ObjexoomShell />` cleanly.
- [x] **B1.5** `pnpm test` (vitest unit) green — 12 suites / 163 tests.
- [x] **B1.6** `pnpm test:e2e:screenshots` produces the 5 canonical PNGs.
- [x] **B1.7** `pnpm assets:fbx-to-glb` regenerates GLBs from `references/`. Shipped: fixed stale `public/objexoom/models/` output paths → `public/assets/models/` (matches AO.2 reorg); created `docs/ASSET_PROVENANCE.md` documenting all 10 FBX→GLB jobs with pack origin + license + re-extraction recipe; script runs idempotent (10/10 skipped on second run, 0 failed); `pnpm assets:verify-runtime` confirms all 27 wired URLs resolve.

### B2 — Mobile + CI

- [x] **B2.1** Android scaffold + APK + AVD smoke shipped. `cap add android` produced the Android Studio project (54 files, 994 LOC); `pnpm build && pnpm cap:sync` runs clean; `./gradlew assembleDebug` builds `app-debug.apk` (15.8 MB); `aapt dump badging` reports `package: name='com.objexiv.objexoom'` + `launchable-activity: name='com.objexiv.objexoom.MainActivity'`; on the running `emulator-5554` (Pixel-class AVD, arm64-v8a, boot_completed=1) `adb install` succeeded, `adb shell monkey ... LAUNCHER 1` foregrounded the app, `dumpsys window` confirmed `mCurrentFocus=com.objexiv.objexoom/.MainActivity` and `topResumedActivity` matches; `adb shell screencap -p` produced a 1.0 MB PNG showing the OBJEXOOM landing screen (title in Black Ops One, menu visible, design tokens applied) — verified visually. ci.yml already conditionally builds the APK and uploads it as `app-debug-apk` (now will trigger since `android/` is tracked). .gitignore scoped to exclude only build outputs (`android/build/`, `android/app/build/`, `android/.gradle/`, `android/local.properties`); the source tree is tracked.
- [x] **B2.2** `.github/workflows/ci.yml` runs lint + check + test + build on PRs.
- [x] **B2.3** `.github/workflows/release.yml` runs release-please on push to main.
- [x] **B2.4** Rewrote `.github/workflows/cd.yml` from the broken `workflow_run`-after-Release stub into a full build+deploy pipeline. Triggers on `push.tags: ['v*']` + `workflow_dispatch: {}`. Build job uses `actions/configure-pages@v5` + `actions/upload-pages-artifact@v3`; deploy job uses `actions/deploy-pages@v4`. `actionlint` exits 0. Local `pnpm build:pages` produces `dist/index.html` with assets resolving to `/objexoom/assets/index-*.{js,css}` (correct BASE_URL prefix); `dist/assets/` contains all 27 GLBs (10.10 MB) verified by `pnpm assets:verify-runtime`. PWA manifest + icons + favicon all copy through to `dist/` with `./`-relative refs that resolve cleanly under `/objexoom/`.
- [x] **B2.5** `.github/dependabot.yml` weekly group non-major + major.

### B3 — Standalone-repo cutover (HISTORICAL; this IS the game repo)

All complete on 2026-05-13. `objexiv/objexoom` is the canonical home of OBJEXOOM. No further action — items preserved as audit trail.

- [x] **B3.1** Easter-egg dir, public assets, references, scripts, tests, spec removed from `objexiv/objexiv`.
- [x] **B3.2** Pruned deps no longer needed in objexiv (three, @react-three/*, postprocessing, tone, yuka, fbx2gltf, sharp).
- [x] **B3.3** Archive tag `archive/objexoom-easter-egg` preserved on `objexiv/objexiv`; branch deleted local + remote.
- [x] **B3.4** Standalone repo `objexiv/objexoom` is the source of truth; objexiv main is clean.

### DS — Design system rollout

- [x] **DS.1** Token sources: `src/design-tokens/{colors,typography,spacing,motion,index}.ts`.
- [x] **DS.2** CSS mirror: `app/tokens.css` with `--obx-*` custom properties.
- [x] **DS.3** Self-hosted horror-tactical fonts: Black Ops One + Rajdhani, 12 woff2 files, declared in `app/fonts.css`.
- [x] **DS.4** Dependabot grouped non-major + major per ecosystem.
- [x] **DS.5** Wire tokens into `ObjexoomHUD` (replace hardcoded hex/rgba with `var(--obx-…)`).
- [x] **DS.6** Wire tokens into `ObjexoomShell` landing / mission-complete / game-over overlays.
- [x] **DS.7** Wired tokens into every scene material. Zero literal hex codes remain in `src/scene/**/*.tsx`. Added 14 semantic anchors to `OBJEXOOM_PALETTE` (wallShadow/wallBase/wallVariantCool/wallVariantWarm/wallVariantNeutral/wallEmissive/door/flashlightWarm/weaponMetalLight/weaponMetalDark/ammoBrass/chestWood/chestWoodDeep/portalTeal/portalRose).
- [x] **DS.8** Apply Black Ops One to all heading-class HUD elements.
- [x] **DS.9** Re-shoot the 5 canonical screenshots with the new typography + tokens applied.

### AO — Asset organization

- [x] **AO.1** Inventory current `public/` layout, document the convention in `public/README.md`.
- [x] **AO.2** Move every existing GLB under `public/assets/models/{enemies,weapons,props}/`.
- [x] **AO.3** Bundle horror enemy GLBs (sewerfiend, plague_doctor, elk_demon, abomination ×2, anomaly, horned, nun, alien, clown ×2) under `public/assets/models/enemies/horror/`.
- [x] **AO.4** Bundled 5 slasher weapon GLBs under `public/assets/models/weapons/slasher/`. Updated `src/models.ts` melee URL to `slasher/melee_machete.glb`, updated all 5 jobs in `scripts/convert-fbx.mjs`, updated provenance + inventory docs. `pnpm assets:verify-runtime` resolves all 27 URLs; canonical screenshots unchanged.
- [x] **AO.5** Shipped PWA manifest + favicon set. `public/manifest.webmanifest` declares name/short_name/description/start_url/scope, `display: fullscreen`, `theme_color`+`background_color`=`#03050b` (`--obx-bg-void`), 192×192 + 512×512 + maskable-512 icons; `public/favicon.ico` + `favicon-32.png` + `apple-touch-icon.png` (180×180). `scripts/generate-pwa-icons.mjs` rasterizes the OBJEXOOM stacked-wordmark SVG via Playwright Chromium (idempotent, runs via `pnpm assets:pwa-icons`). **D14 amends original Lighthouse PWA-category criterion** — Lighthouse 12 retired that category. Verified via current Lighthouse 13: best-practices=100, accessibility=100, seo=91, performance=38 (FPS game, expected). Best-practices + a11y both ≥ 90 per D14.
- [x] **AO.6** `index.html` head references the manifest + all favicon sizes + apple-touch-icon + apple-mobile-web-app-* meta + theme-color synced to `#03050b`. Verified via `curl http://localhost:5191/manifest.webmanifest` returns `Content-Type: application/manifest+json`.

### PARITY — DOOM reference clone

Full audit: [`docs/PARITY.md`](../docs/PARITY.md).

- [x] **PA-MOD2** Visually verify all 5 screenshot poses render cleanly post-extraction.
- [x] **PA1** ManyEnemies spawner wired in `src/refLevel.ts`.
- [x] **PA9** Shell ejection on shotgun fire (`ShellEjectField` active).
- [x] **PA10** Weapon recoil offset on viewmodel (per-weapon `RECOIL_DISTANCE`).
- [x] **PA11** Body-part physics: gravity arc + spin on death (`BodyPartField` active).
- [x] **PA-FULL** End-to-end deep dive — complete; result captured in `docs/PARITY.md`.
- [x] **PA16 / E12** Adaptive resolution via `gl.setPixelRatio` on low FPS. Shipped 57dd8fa. **100% reference parity reached.**
- [x] **PA9b** Extended shell ejection to chaingun fires. Shotgun ejects one large shell per pull; chaingun ejects a smaller (0.6× scale) shell on every pulse. `ShellEjectField` per-shell despawn cap raised to 80 to handle the chaingun rate.
- [x] **PA-MOD7** Muzzle light at the actual barrel tip rather than camera-relative. Shipped per D11: `WeaponModel.muzzleBboxFrac` is a per-weapon [fx,fy,fz] ∈ [0,1]³ lerped against the GLB's runtime Box3 to land at the visible barrel tip; `WeaponViewmodel` renders a `<group>` at that point and surfaces it via an `onMuzzleAnchor` callback; `ObjexoomScene`'s muzzle-flash point light reads `anchor.getWorldPosition()` each frame (falls back to camera.position when no anchor is registered yet, preserving the prior behavior for test harnesses). 5 unit tests in `objexoom-muzzle-anchor.test.ts` pin the authoring contract (frac in bounds, at least one axis ≥ 0.9 marking the tip, the other two axes near center, lerp produces a non-degenerate position distinct from bbox center). Canonical screenshots unchanged — flash position only differs during fire frames, none of the 5 canonical poses capture a fire frame.

### ELEVATION — beyond reference parity

Full roadmap: [`docs/ELEVATION.md`](../docs/ELEVATION.md). Specs in [`docs/PRD.md`](../docs/PRD.md).

**Phase 1 — Critical infra (DONE):**

- [x] **E12** Adaptive resolution via `gl.setPixelRatio` (PA16). Shipped 57dd8fa.
- [x] **E9** sql.js persistent run history. Shipped 5d74778.

**Phase 2 — Mechanical elevation:**

- [x] **E1** Full melee weapon slot — wired BLADE/machete viewmodel + whoosh sfx + DRY'd ownedWeapons. Shipped 8d71475.
- [x] **E5** Destructible barrels with AoE damage. Shipped 688104d. 5-variant skin pool (4 metal + 1 wooden), HP=3, 2.5-tile AoE, 35dmg to enemies / 3dmg to player, chain reactions via queue. 14 unit tests in `objexoom-barrels.test.ts`.
- [x] **E6** Shipped secret switches + hidden wall reveals (step-1 slice). New `src/secrets.ts` owns the data model + `pickRaySwitch(origin, dir, list, maxDist)` (ray-disc hit-test, mirrors `pickRayBarrel`). `ObjexoomSectorMap` gains optional `secrets` field; `refLevel.ts` synthesizes one secret per ref level (id = `index * 100 + 1`, switch + lifting wall placed near level center). `fireResolution.ts` hit-tests switches FIRST (player-intent target — wins over barrels + enemies); on hit, flips `triggered=true`, dispatches typed `secretTriggered` event, pellet consumed. New `src/scene/entities/SecretField.tsx` renders the switch decal (color/emissive flip post-trigger) + lifting wall (900 ms ease, mirrors RealDoor pacing). `playDoor()` + `playDoorTick()` fire on trigger. 13 unit tests in `objexoom-secrets.test.ts` pin hit-detection contract (radius, range, perpendicular cutoff, behind-origin reject, triggered-becomes-inert, closest-of-many) + per-level secret synthesis (3 levels × distinct positions, unique ids). 204 unit + 5 browser + 5 e2e screenshot tests green; canonical screenshots unchanged (the secret is offscreen at the canonical poses).

**Phase 3 — Visual elevation:**

- [x] **E3** Shipped decorative sector prop scatter consuming the COV4 pool. New `src/scatter/propScatter.ts` exports `spawnProps(map, archetype)` — deterministic per-sector rejection sampling that picks 2-5 props per sector inside the polygon, honoring a 4-tile skip-radius from spawn/exit/key anchors and a 1.4-tile minimum inter-prop spacing (matches PRD §E3 acceptance: "no visible repeats within one FOV"). Each PropInstance carries a stable id (`sectorId * 1000 + i`), Vec2 position, deterministic yaw, and a reference to the PropDef. The PRNG is seeded with `map.seed XOR 0x50524F50` ("PROP" tag) so it diverges from lampScatter's "LMP" tag — no accidental sequence overlap between systems. Step-1 slice: all sectors default to `"corridor"` archetype until E13 ships per-map archetype selection. New `src/scene/entities/PropField.tsx` renders one cloned mesh per instance with yaw applied; preloads every prop URL on module evaluation. ObjexoomScene mounts `<PropField props={propsRef.current} />` alongside `<LampField>`. Also folded simplifier finding from COV4 review: removed the unused `scale?: number` field from PropDef (YAGNI — E3 didn't surface a use case for it), derived `PROP_ARCHETYPES` from `Object.keys(POOLS)` instead of duplicating the union, tightened the propPool test to `ALL_PROPS.length === 30`. 11 new propScatter unit tests pin determinism, archetype-bucket-only picks, skip-radius, in-sector spacing, per-sector cap, unique ids, yaw range, grid-map empty case. 237 unit + 5 browser tests green. 5/5 e2e screenshots green; props sit in sector interiors offscreen from the canonical 4-tile-anchored poses, so canonical refs are byte-stable. Runtime: visible in `pnpm dev` once the camera drifts into a sector body. E3 step-2 (registering blocking props with the collision list) is a follow-on item to be opened when a player report or visual check demands it.
- [x] **E4** Lit lamps with shadow projection — wired on top of COV1's scatter. `spawnLamps` now flips `on=true` for the first `MAX_LIT_LAMPS` (8) lamps; `LampField` renders a scoped `<pointLight castShadow>` per lit lamp (distance=6, decay=1.8, color=`flashlightWarm`, 512² shadow map for mobile budget, shadow-bias=-0.0005). Lamps past the cap render the OFF variant with zero light cost. "Don't double-light" handled naturally: the lit lamp's ON GLB has self-emissive material at its native luminance; the pointLight sits 1.4 units above the lamp lighting floor/walls, not the lamp surface itself; the flashlight SpotLight only contributes diffuse fill so the emissive surface stays at native brightness regardless of flashlight aim. Updated unit test pins the lit-prefix contract (`first MAX_LIT_LAMPS lamps on=true, rest off`). 216 unit + 5 browser + 5 e2e screenshot tests green; canonical screenshots unchanged (lit lamps are positioned at sector centroids offscreen from the canonical poses).
- [x] **E2** Shipped boss enemies (step-1 slice). New `Enemy.tier?: "boss"` discriminant; `BOSS_HP_MULTIPLIER = 4` (PRD range 3-5); `BOSS_VISUAL_SCALE = 1.6`. New `pickBossSpawnIndex(map)` picks the enemy spawn farthest from playerSpawn deterministically — that's the "final sector" per PRD §E2. `spawnEnemies` flips exactly one enemy per map to `tier: "boss"` and quadruples its HP. EnemyMesh applies the visual-scale multiplier so bosses render visibly bigger using their existing horror skin roster (no new GLBs needed). Portal-unlock gate in ObjexoomScene now requires `hasKey && allBossesDead && distance < threshold`; the existing skeleton/imp/wraith standard-enemy spawns are unchanged. 9 new unit tests pin: pickBossSpawnIndex picks the farthest spawn, deterministic, returns -1 on empty maps, exactly one boss per refLevel, HP multiplier exact, standards retain base HP, multiplier in PRD range, visual scale > 1, deterministic boss assignment. 260 unit + 5 browser tests green. 5/5 e2e screenshots green — the test harness's `killAllEnemies()` clears bosses too so the win path holds. Step-1 deferred items (HUD "BOSS APPROACHES"/"BOSS DEFEATED" banner + boss-specific aggro alert + death stinger SFX) tracked as next-step polish; the core mechanical gate is shipped.

**Phase 4 — Polish + variety:**

- [x] **E13 step-1** Shipped archetype assignment + per-archetype prop pool. New `src/archetype.ts` exports `ARCHETYPE_NAMES` (canonical 5-archetype order) + `pickArchetype(map)` (deterministic `ARCHETYPE_NAMES[map.seed % 5]`). ObjexoomScene now picks archetype per map (memoized on `map`) and feeds it to `spawnProps` instead of the hardcoded "corridor" — so refLevel 0 (seed 0) still uses corridor, refLevel 1 → arena, refLevel 2 → courtyard, and per-run procedural maps cycle through all 5. Also folded reviewer findings from E2: (1) ExitPortal + RealDoor's visual `unlocked` prop now reads `hasKey && allBossesDead` via a new useState flag synced from the per-frame loop — previously the portal *looked* open the moment the key was collected while the boss was still alive, creating incoherent player feedback; (2) `pickBossSpawnIndex` initial `bestDistSq` changed from -1 to `Number.NEGATIVE_INFINITY` so the picker doesn't silently depend on loop-start index. 6 new archetype unit tests pin: 5-archetype canonical order, seed → ARCHETYPE_NAMES[seed%5] mapping, all 5 reachable across seeds 0..4, deterministic, every refLevel yields a valid archetype, negative-seed safety via unsigned-right-shift. 266 unit + 5 browser tests green. 5/5 e2e screenshots green; canonical screenshots byte-stable (refLevel 0 still uses corridor archetype = the prior default). Step-2+ axes (lighting palette + enemy mix + SFX ambient bed per PRD §E13) tracked as follow-on commits.
- [x] **E7 step-1** Shipped water sectors + wading slowdown. New optional `MapSector.isWater?: boolean` flag on the engine type. `WATER_SPEED_MULTIPLIER = 0.6` (PRD §E7 exact value). New `isInWaterAt(map, point, cache?)` engine helper for cheap per-frame lookups. PlayerController now multiplies `PLAYER_MOVE_SPEED * dt` by the water multiplier when the camera's XZ is inside a water sector — applied PRE-step so slowdown kicks in immediately on water entry. `refLevel.ts` flags one sector per refLevel via new `pickWaterSectorId` (sector centroid farthest from sector 0's centroid, never sector 0 itself, deterministic). New `src/scene/map/WaterSurface.tsx` renders a UV-scrolled semi-transparent indigo plane just above the water sector's floor (0.02 lift to avoid z-fighting with floor + modular tiles); the texture is a procedural 64×64 RGBA noise pattern with sinusoidal banding that scrolls on both axes at different rates (0.04 u/s, 0.025 v/s) — step-2 will swap the procedural texture for the actual `clear+sea+water-2048x2048.png` from the PSX-Ocean-Surface pack. SectorMapGeometry mounts `<WaterSurface />` for every water sector. 8 new unit tests pin: every refLevel has exactly one water sector, the water sector isn't sector 0, isInWaterAt true at water centroid + false on dry land + false on grid maps + false outside any sector, WATER_SPEED_MULTIPLIER in (0,1) and exactly 0.6. 274 unit + 5 browser tests green. 5/5 e2e screenshots green. Splash bursts on water entry/exit are step-2 polish.
- [x] **E8** Shipped Flamethrower as the 5th WeaponId slot. New `WeaponId = ... | "flamethrower"` discriminant. WEAPONS.flamethrower spec: 100ms cooldown (PRD §E8 exact), 5 pellets × 0.42 rad spread = visible cone, 10 damage/pellet × 5 = 50 burst damage per tick, 8-tile range (shorter than shotgun for close-quarters identity), 1 ammo/shot, 30-shot pickup, fire-orange muzzle (ROLE.actionLava = ember-400 `#ff7518`). The held-trigger continuous-fire mechanic reuses the existing cooldown gate in fireResolution.ts — fire input fires every 100ms while held, just like chaingun but with multi-pellet cone. WEAPON_MODELS.flamethrower points at `public/assets/models/weapons/flamethrower.glb` (copied from `references/Flamethrower.glb`, 80 KB). New `playFlamethrower` sfx hook reuses shotgunSynth at 32n duration so 100ms-spaced fires overlap into continuous hiss instead of discrete blasts. Going-back phase H8 auto-grants the flamethrower alongside chaingun + shotgun + 40 ammo. RECOIL_DISTANCE.flamethrower matches chaingun (0.025) for the rapid-fire bob feel. HUD WEAPON_ORDER auto-renders the 5th slot since the order is single-source — visible in canonical refs as "1 BLADE | 2 PISTOL | 3 CHAINGUN | 4 SHOTGUN | 5 FLAMETHROWER". 6 new unit tests pin: WeaponId includes flamethrower, spec shape (cooldown=100, pellets>1, spread>0.2, finite ammo, short range), 5th slot in WEAPON_ORDER, hudHotkey=5, GLB URL pattern, muzzleBboxFrac (PA-MOD7). 329 unit + 5 browser tests green. 5/5 e2e screenshots green. Canonical refs refreshed for the 5-slot HUD row. Step-2 polish (dedicated particle-stream visual via ParticleBurstField fire-themed bursts) is follow-on; the core mechanical loop (hold trigger → 100ms cone-AoE → fuel ammo) is shipped.
- [x] **E11** Shipped per-archetype ambient SFX bed + phase-reactive volume. Extended `src/sfx.ts` with `AmbientArchetype` type + `ARCHETYPE_AMBIENT` table mapping each E13 archetype to a distinct pitch + base volume (corridor=C1/-32, arena=G1/-30 brighter, courtyard=D1/-34 open-air, sewer=A0/-28 oppressive sub-bass, library=E1/-36 delicate near-silent). New `setAmbientArchetype(arch)` re-triggers the drone at the new pitch (idempotent — no-op if archetype unchanged). New `setAmbientPhase(phase)` swells the drone +6dB when `phase === "going_back"` via Tone.js `volume.rampTo` 0.8s ramp. ObjexoomScene mounts two new useEffects that push the archetype (from E13's pickArchetype) and phase (from the existing phaseRef) to sfx. 7 new unit tests pin: default corridor/out, archetype change → pitch change, +6dB phase boost, phase=out clears swell, all 5 archetypes have distinct pitches, all archetype base volumes <0dB (quiet bed, not foreground), idempotent setAmbientArchetype. 283 unit + 5 browser tests green. 5/5 e2e screenshots green. Step-2 polish (distant creature growl/drip/wind sample layers as separate channels) is follow-on; the core mechanical contract (per-archetype identity + going_back reactivity) ships now.
- [x] **E10** Shipped 3D HUD key indicator. New `src/scene/hud/HudKey3D.tsx` mounts a 56×56 r3f Canvas in the HUD overlay (top-right, pointer-events: none, no WebGL cost when `hasKey` is false — Canvas only renders when the key is collected). The key model is built from Three.js primitives (torus head + cylinder shaft + 2 box teeth) so we don't depend on an unwired GLB — fast to load, faithful to the PRD intent. Spins on Y at 1.4 rad/s with a 0.12-rad pitch wobble; material is amber-emissive metal. Damage-flash: HUD listens to `playerHit` and sets `keyFlashUntil = now + 250ms` — HudKey3D's useFrame lerps emissive from amber → blood (ROLE.actionDamage = `#991b1b`) and back over the window. 2 new unit tests pin the hasKey-gated null return (no Canvas mount → no WebGL cost when pre-pickup). 276 unit + 5 browser tests green. 5/5 e2e screenshots green; visual check: ingame-flashlight-on (pre-key) shows no key model in top-right corner per gating; going-back-strobe (post-key) shows the model in the corner. The "etc" portion of PRD §E10 (additional 3D HUD elements like ammo readouts) is follow-on polish; the key indicator is the load-bearing piece per acceptance.

### COV — 3DPSX asset coverage maximization (user directive 2026-05-14)

> "I want as much possible value from ALL the PSX assets — anything
> that makes sense in a level."

Every category below is a directive item: wire at least one
gameplay-meaningful feature that consumes a multi-variant pool from
that category. Seeded id picks the variant so the same instance is
visually consistent across reloads.

- [x] **COV1** Shipped PSX Mega Pack II Light Sources scatter (step-1 slice). Copied all 10 lamp GLBs (5 shapes × off/on, 6.5 MB total) into `public/assets/models/props/lamps/` directly from `/Volumes/home/assets/3DPSX/PSX Mega Pack II v1.8/Light Sources/` (pre-built — no FBX conversion needed). New `src/lampScatter.ts` owns `LAMP_VARIANTS_OFF`/`LAMP_VARIANTS_ON` URL pools + `LampInstance` runtime type + `spawnLamps(map)` deterministic per-sector scatter (skips sectors within 3 tiles of spawn/exit/key, ≥ 2 distinct variants guaranteed when ≥ 2 sectors). New `src/scene/entities/LampField.tsx` renders one cloned mesh per instance; preloads all 10 variants via `useGLTF.preload`. `MAX_LIT_LAMPS=8` budget exported for E4 to consume. Lamps spawn with `on=false` (decorative geometry); **E4 will flip the lit subset's flag and wire the scoped pointLights** — the lane interplay was honored (COV1 stages, E4 lights). 12 unit tests pin determinism, variant ≥2, grid-map empty case, skip-radius respected. `verify-runtime-assets.mjs` extended to scan `src/lampScatter.ts` too; 37 URLs (was 27) all resolve, 16.58 MB total. 216 unit + 5 browser + 5 e2e screenshot tests green; canonical screenshots unchanged (lamps offscreen at canonical poses, will surface naturally on ingame frames once E4 lights them).
- [x] **COV2 step-1** Shipped Large Props & Machinery pool (asset-enabler). Curated 10 hero-sized GLBs (cage, chimney, distillery, 2× electrical equipment, machinery, portal, pipe, shipping container, pipes header, 8.2 MB total) from the 52-file PSX Mega Pack II "Large Props & Machinery" pack. New `src/largeProps.ts` exports `LargePropDef` interface + `LARGE_PROPS` array (mix of blocking + non-blocking — machinery + shipping container are blocking because their physical scale makes "walk through them" feel wrong; the rest stay collision-flat) + `pickLargePropDef(hash)` deterministic picker. PRD §COV2 acceptance "some collision-blocking, some pass-through" pinned via a test asserting both populations exist. 8 new unit tests pin ≥6 acceptance, URL pattern, id uniqueness, blocking/non-blocking split, determinism, full reachability, negative-hash safety. `scripts/verify-runtime-assets.mjs` extended to scan `src/largeProps.ts` — 109 URLs (was 99) resolve. 314 unit + 5 browser tests green. Step-2 (1-2 per archetype-appropriate sector as anchor pieces — different placement shape from COV4's 2-5-per-sector scatter) is follow-on.
- [x] **COV3 step-1** Shipped modular asphalt floor tiles on refLevel 0. New `docs/COV3-ARCHITECTURE.md` enumerates the 4-step plan (floors → walls → doorway cutouts → archetype-specific sets) so future steps land against a known shape. New `src/scatter/floorTiles.ts` exports `spawnFloorTiles(map)` — deterministic grid-tiled scatter (snap-to-`TILE_SIZE=2` grid, accept tiles whose center is inside the sector polygon) seeded with `map.seed XOR 0x464C5254` ("FLRT" tag) so it diverges from prop/lamp scatter sequences. New optional `useModularFloor: boolean` field on `ObjexoomSectorMap`; refLevel 0 sets it true, refLevels 1+2 stay false (procedural floor). SectorMapGeometry now conditionally omits the procedural floor `<shapeGeometry>` when the flag is set AND the sector isn't lava (lava keeps its emissive procedural shape — there's no lava-tile asset in the modular pack). New `src/scene/entities/FloorTileField.tsx` renders one cloned mesh per tile with a quarter-turn yaw for variety. Copied 4 asphalt GLBs (`asphalt_hr_1.glb`, `asphalt_hr_1_large.glb`, `asphalt_hr_2.glb`, `asphalt_hr_3.glb`, 2.1 MB total) from `/Volumes/home/assets/3DPSX/PSX Mega Pack II v1.8/Modular Structures/` into `public/assets/models/structures/`. 12 new unit tests pin the gating contract (only refLevel 0 tiles; refLevels 1+2 empty; grid maps empty), determinism, polygon-containment of every tile center, variant/rotation enum ranges, ≥2 variants used, id uniqueness, floorHeight match. Folded E3 reviewer findings into the same commit: extracted `sampleAcceptablePoint` + `nearAny` predicates from propScatter (drops the triple `tooClose` flag-juggling), named `ID_STRIDE=1000` constant so the formula's invariant (`PROPS_PER_SECTOR_MAX <= ID_STRIDE`) is documented. `scripts/verify-runtime-assets.mjs` extended to scan `src/scatter/floorTiles.ts` — 71 URLs (was 67) resolve. 251 unit + 5 browser tests green. 5/5 e2e screenshots green; canonical refs refreshed — `ingame-flashlight-on.png` now shows visibly textured asphalt floor instead of the prior flat purple shape. Visual win: the scene reads like a real industrial space rather than colored boxes. COV3 step-2 (modular wall pieces), step-3 (doorway cutouts), step-4 (archetype-specific tile sets, E13 dependency) tracked in the new architecture doc.
- [x] **COV4** Shipped PSX Mega Pack II Props scatter pool. Curated 30 GLBs from the 137-file pack into `public/assets/models/props/scatter/` (6.4 MB), with each chosen for archetype affinity (industrial barrels, crates, cement bags, pipes, gears, benches, shelves, etc). New `src/scatter/propPool.ts` exports `PROP_CATALOGUE` (30 named entries with stable id + BASE_URL-resolved URL + boolean blocking flag) and `POOLS` — five per-archetype buckets keyed by `PropArchetype = "corridor" | "arena" | "courtyard" | "sewer" | "library"` per `docs/PRD.md §E13` (re-aligned from the directive's earlier "kitchen/factory/temple/sewer" draft per the DESIGN > … > directive single-source chain). Each bucket has 10-11 entries; intentional overlap (a metal barrel reads in corridor + sewer + courtyard) reflects real warehouse semantics. Inline static `A("/assets/…")` URLs (no template-literal interpolation) so `verify-runtime-assets.mjs` parses them statically. 10 new unit tests pin: ≥10 unique props, every bucket ≥10, no orphan bucket entries, no in-bucket duplicates, blocking flag round-trip. `verify-runtime-assets.mjs` extended to scan `src/scatter/propPool.ts` — 67 URLs (was 37) resolve, 22.94 MB total. 226 unit + 5 browser tests green. Also folded the E4 reviewer trio's finding: `flashlightWarm` token was misaliased to `SCALE.parchment[50]` (`#fafbff`, cool near-white) while its comment said it should be `#fef3c7` (warm amber lens) — fixed the token to its documented intent, which restores warm lamp/flashlight/pickup lighting per E4 design notes. Three consumers (Flashlight SpotLight, LampField pointLight, PickupMesh emissive) now read true amber.
- [x] **COV5 step-1** Shipped debris variant pool (asset-enabler). Curated 10 GLBs (4 loose bricks + 2 stacked-brick groups + 2 debris piles + 2 gravel piles, 1.7 MB total) from the 34-file PSX Mega Pack II Debris & Misc pack — full pack has many redundant brick orientations, this curation covers the visual spread without bloat. New `src/debris.ts` exports `DEBRIS_VARIANTS` + `pickDebrisUrl(hash)` (deterministic via `(hash >>> 0) % DEBRIS_VARIANTS.length`). 7 new unit tests pin ≥5 acceptance, URL pattern, uniqueness, determinism, full reachability, negative-hash safety. `scripts/verify-runtime-assets.mjs` extended to scan `src/debris.ts` — 99 URLs (was 89) resolve. 306 unit + 5 browser tests green. Step-2 (sector-body debris scatter in propScatter or a sibling) is follow-on.
- [x] **COV6 step-1** Shipped decal variant pool (asset-enabler). Copied 12 PSX Mega Pack II Decals (4 graffiti + 8 posters, 580 KB total) into `public/assets/models/props/decals/`. New `src/decals.ts` exports `DECAL_VARIANTS_GRAFFITI`, `DECAL_VARIANTS_POSTER` (disjoint, split for archetype-aware step-2 picks — sewer/corridor prefer graffiti, library/arena prefer posters), `DECAL_VARIANTS_ALL` (union), and `pickDecalUrl(tileHash)` deterministic picker. 9 new unit tests pin: ≥12 total, graffiti+poster disjoint, ALL is union, URL pattern, uniqueness, determinism, full reachability, negative-hash safety. `scripts/verify-runtime-assets.mjs` extended to scan `src/decals.ts` — 89 URLs (was 77) resolve. 299 unit + 5 browser tests green. Step-2 (≥3 decals per sector mounted onto wall faces by tile hash inside SectorMapGeometry) is follow-on; this commit ships the asset+picker so step-2 is a clean renderer change.
- [x] **COV7 step-1** Shipped door variant pool. Copied 6 GLBs (`door_hr_6.glb`, `door_hr_8.glb`, `door_hr_12.glb`, `door_hr_13.glb`, `door_hr_14.glb`, `gate_1.glb`, 840 KB total) from `/Volumes/home/assets/3DPSX/PSX Mega Pack II v1.8/Doors & Gates/` into `public/assets/models/props/doors/`. New `src/doors.ts` exports `DOOR_VARIANTS` + `pickDoorUrl(seed)` (deterministic via `(seed >>> 0) % DOOR_VARIANTS.length`). 7 new unit tests pin: ≥3 variants ship (acceptance), every URL resolves to /assets/models/props/doors/*.glb, URLs unique, deterministic, all variants reachable across seeds 0..5, negative-seed safety. `scripts/verify-runtime-assets.mjs` SOURCE_FILES extended to scan `src/doors.ts` — 77 URLs (was 71) resolve. 290 unit + 5 browser tests green. Step-2 (swap RealDoor's procedural boxGeometry for the picked GLB) is follow-on; this commit ships the asset-enabler so step-2 is a clean renderer swap.
- [x] **COV8 step-1** Shipped trap variant pool (asset-enabler). Copied 10 curated trap GLBs (3 spikes + 3 blades + 2 rolling/log hazards + 2 lever/base trigger components, 248 KB total) from `/Volumes/home/assets/3DPSX/Props/Traps/` (20-file pack) into `public/assets/models/props/traps/`. New `src/traps.ts` exports `TrapKind = "spike" | "blade" | "rolling" | "trigger"` discriminated union + `TrapDef` interface + `TRAPS` array (every entry tagged by kind) + `trapsByKind(kind)` filter + `pickTrapDef(hash)` deterministic picker. Coverage test pins all 4 kinds reachable. 9 new unit tests pin ≥6 acceptance, 4-kind coverage, URL pattern, id uniqueness, filter correctness, picker determinism + range + negative-safety. `scripts/verify-runtime-assets.mjs` extended — 149 URLs (was 139) resolve. 369 unit + 5 browser tests green. Step-2 (tick damage on player overlap + lever-disarm pairing with E6 secrets) is follow-on; this commit ships the asset pool and the kind taxonomy so step-2 lands as a clean engine addition.
- [x] **COV9 step-1** Shipped 7-skin melee roster + `pickMeleeSkin` picker (asset-enabler). Added `Sword.glb` + `Cleaver.glb` from `references/` (loose extras unpacked but unwired) as `public/assets/models/weapons/slasher/melee_sword.glb` + `melee_cleaver.glb`. The 5 slasher pack originals (axe/chainsaw/knife/machete/meathook) were already wired by AO.4. New `src/meleeSkins.ts` exports `MELEE_SKIN_URLS` (7-entry array with machete at index 0 for E1 back-compat) + `pickMeleeSkin(seed)` deterministic picker via `(seed >>> 0) % MELEE_SKIN_URLS.length`. 9 new unit tests pin ≥4 skins acceptance, machete-at-index-0 back-compat (so canonical screenshots with seed 0 stay byte-stable), URL pattern, uniqueness, determinism, full reachability, negative-seed safety. `scripts/verify-runtime-assets.mjs` extended to scan `src/meleeSkins.ts` — 115 URLs (was 109) resolve. 323 unit + 5 browser tests green. Step-2 (swap `WEAPON_MODELS.melee.url` at WeaponViewmodel mount time via the picker output) is follow-on; this commit ships the asset pool so step-2 is a clean runtime-resolution change.
- [x] **COV10 step-1** Shipped vehicle wreck pool (asset-enabler). Copied 3 PS1-RVS GLBs (RV1/RV2/RV3, 128 KB) from `/Volumes/home/assets/3DPSX/Vehicles/PS1-RVS/` into `public/assets/models/props/vehicles/`. New `src/vehicles.ts` exports `VEHICLE_VARIANTS` + `pickVehicleUrl(seed)`. 7 new unit tests pin ≥1 acceptance, all 3 shipped, URL pattern, uniqueness, determinism, full reachability, negative-seed safety. `scripts/verify-runtime-assets.mjs` extended — 119 URLs (was 116). 337 unit + 5 browser tests green. Step-2 (place one wreck in courtyard-archetype sectors per PRD §COV10) is follow-on.
- [x] **COV11 step-1** Shipped nature mega pack (asset-enabler). Copied `Mega_Nature.glb` from `/Volumes/home/assets/3DPSX/Fantasy/PSX MEGA Nature Pack/` into `public/assets/models/props/nature/` (232 KB — surprisingly compact for an aggregate). New `src/nature.ts` exports `NATURE_MEGA_PACK_URL`. 2 new unit tests. 353 unit + 5 browser + 133 URLs verified. Step-2 (SkeletonUtils-clone individuals out for the courtyard-archetype seasonal scatter pass) is follow-on.
- [x] **COV12 step-1** Shipped fantasy loot pool (asset-enabler). Copied 3 hero GLBs (Bottles.glb, Books.glb, Treasure.glb, 8.5 MB total — these are scene-aggregate GLBs containing many sub-meshes, step-2 can SkeletonUtils.clone individuals out) from `/Volumes/home/assets/3DPSX/Fantasy/`. New `src/loot.ts` exports `LootKind = "bottles" | "books" | "treasure"` + `LOOT_URLS` Record + `LOOT_URL_LIST` + `pickLootKind(hash)`. 7 new unit tests. `scripts/verify-runtime-assets.mjs` extended — 132 URLs (was 129). 351 unit + 5 browser tests green. Step-2 (rare drop ~1/refLevel via new PickupKind values) is follow-on.
- [x] **COV13 step-1** Shipped kitchen prop pool (asset-enabler). Curated 10 GLBs (2 bowls + chair + 2 drinks + butter_knife + 4 food packaging, 788 KB) from `/Volumes/home/assets/3DPSX/Props/Kitchen/` into `public/assets/models/props/kitchen/`. New `src/kitchen.ts` exports `KITCHEN_PROPS` + `pickKitchenProp(hash)`. 7 new unit tests. 344 unit + 5 browser + 129 URLs verified. Step-2 (wire into a new "kitchen" archetype or as set-dressing in library/courtyard) is follow-on.
- [x] **COV14 step-1** Shipped NPC chibi pool (asset-enabler). Copied 6 ChibiCharacters GLBs (archer, knight, merchant, ninja, student, basemesh, 22 MB total — full-rigged characters) from `/Volumes/home/assets/3DPSX/Characters/ChibiCharacters/` into `public/assets/models/enemies/npc/`. New `src/npcs.ts` exports `NpcKind` discriminated union + `NPC_URLS` Record + `NPC_URL_LIST` + `pickNpcKind(hash)`. 8 new unit tests. 361 unit + 5 browser + 139 URLs verified. Step-2 (extend `EnemyKind` with `"npc"` variant, FSM treats as ambient — no aggro/LOS/attack, HUB-archetype sectors spawn one chibi per slot) is follow-on; this commit ships the asset pool so step-2 is purely engine work.

These are sequenced by gameplay value (light/structures/props first
because they yield the biggest visual ROI). Re-order any time.

### ARCH — architectural maintenance (raised by PR #12 architect review)

- [x] **ARCH1a** Shipped `src/events.ts` with `ObjexoomEvent` discriminated union over all 14 channels plus `dispatch` + `addObjexoomListener` helpers. Wire format preserved (CustomEvent.detail omits the discriminator `type`), so untyped existing consumers and typed new listeners cross-talk freely — enables ARCH1b's incremental migration. 6 new unit tests pin the round-trip + back-compat contracts (191 unit tests passing).
- [x] **ARCH1b** Migrated every `objexoom:*` call site in production code to `dispatch` / `addObjexoomListener` per D13. Zero `window.dispatchEvent(new CustomEvent("objexoom:` strings remain in `src/` (verified by grep). 8 production files touched (Shell, Scene, HUD, PlayerController, WeaponViewmodel, ParticleBurstField, BodyPartField, ShellEjectField, fireResolution). Topology preserved (no Shell↔Scene→PlayerController ref-callback rewiring); the win is compile-time-typed payloads. 191 unit + 5 browser + 5 e2e screenshot tests green; canonical screenshots unchanged.

- [x] **ARCH2a** Extracted enemy-tick loop to `src/scene/hooks/enemyTickLoop.ts` as a pure `tickEnemyLoop(ctx)` function (not a hook — useFrame plumbing stays where it belongs). ObjexoomScene shed ~96 LOC (891 → 795); the in-frame call site is one line. Behavior byte-identical (185 unit + 5 browser + 5 e2e screenshot tests stay green; canonical screenshots unchanged).
- [x] **ARCH2b** Extracted single-shot resolution to `src/scene/hooks/fireResolution.ts` as a pure `resolveFire(ctx)` function (parallel shape to ARCH2a). The useEffect that wires the `objexoom:fire` listener stays in ObjexoomScene; the body is one call into the helper. ObjexoomScene shed another ~149 LOC (795 → 646; total -245 across ARCH2a+ARCH2b). All 185 unit + 5 browser + 5 e2e screenshot tests green; canonical screenshots unchanged (pure relocation refactor).

### INFRA — supporting infrastructure

- [x] **INF1** WASM/asset sync per arcade-cabinet pattern. `scripts/prepare-web-wasm.mjs` runs at postinstall + prebuild; sql.js WASM copies to `public/assets/wasm/`. Shipped 81ed15d.
- [x] **INF2** Shipped `scripts/copy-public-assets.mjs` post-build asset report. Vite already copies `public/` to `dist/` natively, so the script doesn't re-copy — it walks `dist/assets/` (configurable via `--dir=<path>`) and emits per-category totals sorted by size: files-per-category, bytes-per-category, grand total. Run as part of `pnpm build`, `pnpm build:pages`, and `pnpm build:native` (chained after `vite build` so the dist tree exists). Also wired as standalone `pnpm assets:report`. No byte budgets enforced — informational only, per directive's "NO arbitrary byte budgets — asset weight is tuned deliberately per pickup." Confirmed working: full build outputs 149 files / 57.13 MB total breakdown (46.24 MB models, 8.29 MB sourcemap, 1.84 MB JS, 644 KB wasm, 117 KB fonts, 6.5 KB CSS). Handles missing dist gracefully (warns + exits 0). 351 unit + 5 browser tests stay green.
- [x] **INF3** CI gate `scripts/verify-runtime-assets.mjs` — every GLB referenced by `models.ts` exists at the resolved path. Shipped 81ed15d; byte-budget enforcement was removed in 688104d because the budgets cost us a quality downgrade on barrel.glb (the 6.8 KB Farm barrel got chosen over the 408 KB PSX Mega Pack II metal barrel).

## Verification

Before flipping any [ ] to [x]:

1. `pnpm verify` green (lint + check + test + test:browser + assets:verify-runtime).
2. For any UI/scene change, the relevant canonical screenshot re-shot and visually compared against `docs/assets/objexoom/`.
3. For any new mechanic, at least one unit or browser test pinning the contract.
4. Commit pushed to `feat/objexoom-game-buildout`.

## Self-assessment after every commit

Per `~/.claude/CLAUDE.md` — before opening the next item, ask:

- **Backward:** what shipped, what gap did self-review or the simplification reviewer surface, did anything user-visible regress?
- **Forward:** given the just-shipped learnings, is the next directive item still the right next item? Does its acceptance criterion still hold, or did the prior commit move the goalposts?

Encode forward learnings into the directive before starting the next stage.
